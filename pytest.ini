[pytest]
minversion = 7.0
testpaths = tests
addopts = -q -r a

# ReportPortal placeholders (prefer passing rp_uuid via ENV in CI)
rp_endpoint = https://reportportal.example.com
rp_project = MyProject
rp_launch = MyAutomationSuite
# rp_uuid = <set via CI env var, not here>
